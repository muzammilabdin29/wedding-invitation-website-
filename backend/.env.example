# Comma separated list of allowed origins for CORS, or "*" for all
CORS_ORIGINS=*

# Database URL. Defaults to local SQLite if not set.
# Postgres example: postgresql://user:password@db:5432/wedding
DATABASE_URL=sqlite:///./data/wedding.db
